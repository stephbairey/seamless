services:
  seamless-dashboard:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: seamless-dashboard
    ports:
      - "8420:8420"
    volumes:
      - ../data:/app/data
      - ../app:/app/app
      - "/mnt/c/Users/scbzo/Downloads/MAGIC SORT:/intake"
      - "/mnt/g/My Drive/ALL PROJECTS:/gdrive"
    env_file:
      - ../secrets.env
    environment:
      - PYTHONDONTWRITEBYTECODE=1
    restart: unless-stopped
